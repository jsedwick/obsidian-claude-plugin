---
description: Display recent Obsidian projects
---

Please use the mcp__obsidian-context-manager__list_recent_projects tool with _invoked_by_slash_command: true to display my 5 most recent projects as a numbered list (not a table).

After the tool returns, render the numbered list as your text response so the user sees it — the tool-call panel alone is not sufficient (some chat UIs collapse or hide it). Output ONLY the numbered list — no additional commentary, explanations, or usage tips.
